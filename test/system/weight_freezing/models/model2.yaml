---


input_dimensions:
  height: 8
  width: 8

# layerwise configurations for each layer in the network
layers:
  - name: sparsey
    params:
      num_macs: 16
      mac_grid_num_rows: 4
      mac_grid_num_cols: 4
      num_cms_per_mac: 4
      num_neurons_per_cm: 4
      mac_receptive_field_radius: 1.5
      sigmoid_lambda: 28.0
      sigmoid_phi: 5.0
      saturation_threshold: 0.5

  - name: sparsey
    params:
      num_macs: 4
      mac_grid_num_rows: 2
      mac_grid_num_cols: 2
      num_cms_per_mac: 2
      num_neurons_per_cm: 2
      mac_receptive_field_radius: 1.5
      sigmoid_lambda: 28.0
      sigmoid_phi: 5.0
      saturation_threshold: 0.75

  # persistence -> talk to Dr. Rinkus
  # weight freezing thresholds -> talk to Dr. Rinkus
  # activation thresholds -> talk to Dr. Rinkus
...