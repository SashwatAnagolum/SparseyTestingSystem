---


input_dimensions:
  height: 16
  width: 16

# layerwise configurations for each layer in the network
layers:
  - name: sparsey
    params:
      num_macs: 100
      mac_grid_num_rows: 10
      mac_grid_num_cols: 10
      num_cms_per_mac: 8
      num_neurons_per_cm: 8
      mac_receptive_field_radius: 0.5
      sigmoid_lambda: 28.0
      sigmoid_phi: 5.0
      saturation_threshold: 0.5

  - name: sparsey
    params:
      num_macs: 64
      mac_grid_num_rows: 8
      mac_grid_num_cols: 8
      num_cms_per_mac: 4
      num_neurons_per_cm: 8
      mac_receptive_field_radius: 2.0
      sigmoid_lambda: 28.0
      sigmoid_phi: 5.0
      saturation_threshold: 0.5

  # persistence -> talk to Dr. Rinkus
  # weight freezing thresholds -> talk to Dr. Rinkus
  # activation thresholds -> talk to Dr. Rinkus
...